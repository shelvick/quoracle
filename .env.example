# Required (production). In development, these override the defaults in dev.exs.
# Note: Ecto connects via TCP, so PostgreSQL must be configured with password
# authentication (peer auth will not work). Set a password for your DB user
# if you haven't already.
DATABASE_URL=ecto://postgres:postgres@localhost/quoracle

# Generate with: openssl rand -base64 64 | tr -d '\n'
SECRET_KEY_BASE=

# Generate with: openssl rand -base64 32
CLOAK_ENCRYPTION_KEY=

# Optional (defaults shown)
PHX_HOST=localhost
PORT=4000
POOL_SIZE=10
# PHX_BIND_ADDRESS=127.0.0.1  # 0.0.0.0 or :: to listen on all interfaces
                               # Docker requires 0.0.0.0 (set automatically in docker-compose.yml)
# ECTO_IPV6=true
